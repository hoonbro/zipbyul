# contracts — enum/코드 SSOT + API 계약

Python·Spring·프론트가 공유하는 단일 출처. 코드 문자열을 그대로 쓴다(한글은 표시용).

- `enums.yaml` — 백엔드 기획안 부록 A (interest_type 9종 · event_type · supply_type · trade_type · alert_level 5종 · index_code · 상태값).
- `openapi.yaml` — 서빙 API 계약(OpenAPI 3.1, 15 paths). 비회원·서빙·알림·internal 엔드포인트 + 공통 에러 envelope. 프론트 데이터 레이어/코드젠 기준.
